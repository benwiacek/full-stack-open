```mermaid
sequenceDiagram
    participant browser
    participant server

    browser->>browser: prevents default handling of form submission
    browser->>browser: adds new note to list and re-renders the list
    browser->>server: POST https://studies.cs.helsinki.fi/exampleapp/new_note_spa
    activate server
    server-->>browser: 201 created with JSON {"message": "note created"}
    deactivate server
```